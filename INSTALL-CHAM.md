# 챔대 자동계산기 권한 적용

## 적용 순서
1. Supabase 대시보드에서 해당 사이트 프로젝트의 SQL Editor를 엽니다.
2. 동봉된 `cham-permissions.sql`을 실행합니다. 기존 회원 데이터는 삭제하지 않습니다.
3. 압축파일의 사이트 파일을 기존 호스팅에 업로드합니다. 기존 파일은 먼저 백업하세요.
4. 관리자 계정으로 로그인하고 `admin_users.html`(회원 관리)을 엽니다.
5. 원하는 회원 옆의 **자동계산기 권한 부여**를 누릅니다. 해제할 때는 **자동계산기 권한 회수**를 누릅니다.

관리자 본인도 자동계산기를 이용하려면 본인에게 권한을 부여해야 합니다.
권한은 계정별로 저장하며, 기본값은 권한 없음입니다. 정지 계정은 권한이 있어도 차단합니다.

## 동작
- 한국어·영어 메인 화면과 공통 메뉴에서 승인된 계정에만 자동계산기 링크가 표시됩니다.
- 직접 URL을 열어도 권한이 없으면 계산기 본문 대신 안내가 표시됩니다.
- 계산/추천 실행 전에 DB 권한을 재확인합니다. 열린 화면은 최대 약 15초 후 변경을 반영합니다(백그라운드 탭은 다시 활성화될 때).
- 연결 실패나 SQL 미적용 상태에서도 자동계산기는 숨깁니다.
- 이미 실행 중인 브라우저 계산을 서버에서 취소하는 기능은 없습니다.
- 영어 메인에도 권한 회원용 자동계산기 카드를 추가했습니다.

## 현재 구조의 한계 — 보안 인증 기능은 아님
이번 수정은 기존 로그인 방식에 맞춘 **화면·이용 동선 제한**입니다. 서버 인증 기반의 독점 이용 보장을 제공하지 않습니다.
기존 사이트의 `community_user`는 localStorage에 저장되고 DB 요청은 공통 publishable key를 사용합니다. 사용자나 관리자 신원을 서버가 인증한 세션으로 확인하지 않습니다. DB에서 관리자 여부를 재조회해도 이 한계는 해결되지 않습니다.
개발자 도구로 회원 ID/화면/스크립트를 조작하거나 공개된 계산 코드를 내려받는 우회는 막을 수 없습니다. 기존 DB 정책이 익명 PATCH를 허용한다면 권한 필드도 직접 수정될 가능성이 있습니다.
**이 기능을 위해 RLS를 해제하거나 anon 역할에 추가 쓰기 권한을 주지 마세요.** 권한 변경이 실패하면 실제 DB 정책을 확인해야 합니다. 첨부파일에는 운영 DB 정책이 없어 검증하지 못했습니다.
서버에서 강제하려면 Supabase Auth 등 검증된 로그인으로 전환하고, 관리자만 권한을 변경하는 DB 정책/API와 서버 계산 API를 함께 구현해야 합니다.

## 수정 파일
`index.html`, `en/index.html`, `cham_auto.html`, `en/cham_auto.html`, `admin_users.html`, `ui.js`

추가 파일: `cham-access.js`, `cham-access.css`, `cham-permissions.sql`, 이 안내문.

실제 운영 사이트 배포 및 SQL 실행은 수행하지 않았습니다.
