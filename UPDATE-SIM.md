# 정령 입력 토글 및 계산 속도 수정

## 변경 내용
- 한국어 정령 시뮬레이터·길드전 계산기(오리지널/뉴비)에 기존 방식(드롭다운) / 버튼 방식 토글을 추가했습니다.
- 방식 변경 시 입력값은 유지되며, 선택한 방식은 이 브라우저에서 다음 방문에도 적용됩니다.
- 입력창은 넓은 화면에서 2열, 좁은 화면에서 1열로 배치합니다. 설정 패널처럼 폭이 좁은 영역도 자동으로 1열을 사용합니다.
- 태펜 제외 옵션을 제거했습니다. 보유한 태양 펜던트도 계산 후보에 포함됩니다.
- 1,000개 조합마다 기다리던 처리를 제거하고, 약 100ms 간격으로 진행률을 갱신하도록 바꿨습니다. 실제 화면 갱신 간격은 작업량에 따라 다릅니다.
- 브라우저 작업 양보 API를 사용하며, 지원하지 않는 브라우저에서는 MessageChannel로 대체합니다. 반복 setTimeout의 지연 누적을 피합니다.
- 기존 계산 공식, 후보 선택 및 정렬 기준은 변경하지 않았습니다.
- 계산 실행 버튼, 진행률 %, 챔대 자동계산기 권한 기능은 유지됩니다.

## 적용
기존 사이트 파일을 백업한 후 ZIP의 testonly-main 폴더 안 파일들을 업로드하세요. 앞서 권한 SQL을 적용했다면 추가 SQL은 없습니다. 적용 후 Ctrl+F5로 새로고침하세요.

이번 변경 파일: calculator.html, guild-calculator.js, spirit-simulator.js, spirit-input.js, spirit-input.css.
한국어 계산기 대상이며, en/의 별도 구버전 계산기는 이번 변경 대상이 아닙니다.

## 검증
Edge에서 입력 선택·해제, 드롭다운 입력, 방식 간 값 유지, 방식 선택 저장, 모바일/데스크톱 배치, 100% 완료, 뉴비 페이지 로딩을 확인했습니다.
이전 진행률 수정본과 동일한 입력으로 계산 시간과 결과를 비교했습니다. 실제 계산 시간은 장비·드래곤 수와 기기 성능에 따라 달라집니다.

| 테스트 | 이전 진행률 수정본 | 개선본 |
| --- | ---: | ---: |
| 정령 시뮬레이터, 15,069,888개 조합 | 72.11초 | 1.20초 |
| 길드전, 드래곤 6마리·장신구 12개 | 2.88초 | 0.54초 |

정령 테스트의 결과별 비밸·최종 스탯과 길드전의 선택 드래곤·장신구·비밸·최종 스탯이 전후 일치했습니다. 측정은 로컬 Edge 자동화 환경에서 수행했고 서버 통신 시간은 포함하지 않았습니다.
운영 사이트 업로드는 수행하지 않았습니다.
